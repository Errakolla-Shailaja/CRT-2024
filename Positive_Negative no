#include <stdio.h>
int main() {
    // Write C code here
    int a;
    scanf("%d",&a);
    if(a>0)
    {
     printf("Positive number",a);
    }
    else
    if(a<0)
    {
     printf("Negative number",a);
      }
      else
      {
          printf("Zero",a);
      }
    return 0;
}
